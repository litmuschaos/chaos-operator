# Golang CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-go/ for more details
version: 2
jobs:
  build:
    machine:
        image: circleci/classic:201808-01
        docker_layer_caching: true
    environment:
      K8S_VERSION: v1.12.0
      KUBECONFIG: /home/circleci/.kube/config
      MINIKUBE_VERSION: v1.3.1
      MINIKUBE_WANTUPDATENOTIFICATION: false
      MINIKUBE_WANTREPORTERRORPROMPT: false
      MINIKUBE_HOME: /home/circleci
      CHANGE_MINIKUBE_NONE_USER: true
      SHORT_GIT_HASH: echo ${CIRCLE_SHA1}

    

    working_directory: ~/go/src/github.com/litmuschaos/chaos-operator
    
    steps:
      - checkout
      - run:
         name: Setup kubectl
         command: | 
           echo ${SHORT_GIT_HASH}
           echo ${CIRCLE_SHA1}
           curl -Lo kubectl https://storage.googleapis.com/kubernetes-release/release/${K8S_VERSION}/bin/linux/amd64/kubectl && chmod +x kubectl && sudo mv kubectl /usr/local/bin/
      - run:
          name: Setup minikube
          command: |
            curl -Lo minikube https://storage.googleapis.com/minikube/releases/latest/minikube-linux-amd64  && chmod +x minikube
            sudo install minikube /usr/local/bin
      - run:
          name: Start minikube
          command: |
            sudo -E minikube start --vm-driver=none --cpus 2 --memory 4096 --kubernetes-version=${K8S_VERSION}
      - run: 
          name: Setup ENV
          command: |
            echo 'export GOPATH=/home/circleci/go' >> $BASH_ENV
            echo 'export PATH="$GOPATH/bin:$PATH"' >> $BASH_ENV
            echo 'export REPONAME=litmuschaos' >> $BASH_ENV
            echo 'export IMGNAME=chaos-operator' >> $BASH_ENV
            echo 'export IMGTAG=latest' >> $BASH_ENV
            echo 'export SHORT_GIT_HASH="$(echo ${CIRCLE_SHA1} | cut -b 5)"' >> $BASH_ENV
            echo ${SHORT_GIT_HASH}
            sed '/chaos-operator:latest/s/latest/${CIRCLE_SHA1}/' -i ./deploy/operator.yaml 
            cat ./deploy/operator.yaml
      - run: |
          make deps
      - run: make gotasks
      - run: make dockerops
      - run: docker tag litmuschaos/chaos-operator:latest litmuschaos/chaos-operator:${SHORT_GIT_HASH}
      - run: |
          make test  
      - run: |
          ${GOPATH}/src/github.com/litmuschaos/chaos-operator/buildscripts/push
      
